<% if current_user.class.to_s == "ServiceProvider" %>
  $("#job_feed").html("<%= escape_javascript(render :partial => "jobs/job", :collection => @jobs, :locals => {:public => true}) %>");
  $("#job_feed").append("<%= escape_javascript(paginate @jobs, :remote => true) %>");
<% end %>